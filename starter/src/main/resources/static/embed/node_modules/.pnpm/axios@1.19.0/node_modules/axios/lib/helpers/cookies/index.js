import e from "../../utils/index.js";
import t from "../../platform/index/index.js";
//#region node_modules/.pnpm/axios@1.19.0/node_modules/axios/lib/helpers/cookies.js
var n = t.hasStandardBrowserEnv ? {
	write(t, n, r, i, a, o, s) {
		if (typeof document > "u") return;
		let c = [`${t}=${encodeURIComponent(n)}`];
		e.isNumber(r) && c.push(`expires=${new Date(r).toUTCString()}`), e.isString(i) && c.push(`path=${i}`), e.isString(a) && c.push(`domain=${a}`), o === !0 && c.push("secure"), e.isString(s) && c.push(`SameSite=${s}`), document.cookie = c.join("; ");
	},
	read(e) {
		if (typeof document > "u") return null;
		let t = document.cookie.split(";");
		for (let n = 0; n < t.length; n++) {
			let r = t[n].replace(/^\s+/, ""), i = r.indexOf("=");
			if (i !== -1 && r.slice(0, i) === e) try {
				return decodeURIComponent(r.slice(i + 1));
			} catch {
				return r.slice(i + 1);
			}
		}
		return null;
	},
	remove(e) {
		this.write(e, "", Date.now() - 864e5, "/");
	}
} : {
	write() {},
	read() {
		return null;
	},
	remove() {}
};
//#endregion
export { n as default };
