-- 创建 bytedesk 数据库（如不存在）
CREATE DATABASE IF NOT EXISTS `bytedesk`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- 创建 bytedesk_freeswitch 数据库（如不存在）
CREATE DATABASE IF NOT EXISTS `bytedesk_freeswitch`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
