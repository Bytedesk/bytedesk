import 'package:flutter/material.dart';

class TicketSubmitProvider extends StatelessWidget {
  const TicketSubmitProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        // child: child,
        );
  }
}
