import 'package:flutter/material.dart';

class RateProvider extends StatelessWidget {
  const RateProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
