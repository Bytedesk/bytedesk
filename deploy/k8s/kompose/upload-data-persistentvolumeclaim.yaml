apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: upload-data
  name: upload-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
