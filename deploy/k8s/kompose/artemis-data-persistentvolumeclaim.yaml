apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: artemis-data
  name: artemis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
