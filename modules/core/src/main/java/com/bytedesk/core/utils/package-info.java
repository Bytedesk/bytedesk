/**
 * utils package for shared utility helpers for dates, files, JWT, phone parsing, password handling, and JSON results.
 * utils 包，负责日期、文件、JWT、号码解析、密码处理与 JSON 响应等共享工具。
 *
 * <p>The package currently provides lightweight support classes and extension points around this domain.
 * 该包当前主要提供围绕此领域的轻量支撑类与扩展点。
 *
 * @author bytedesk.com
 */
@NonNullApi
package com.bytedesk.core.utils;

import org.springframework.lang.NonNullApi;