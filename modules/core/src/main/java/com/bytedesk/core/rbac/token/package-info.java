/**
 * token package for token record persistence, token scope and type modeling, and token lifecycle services.
 * token 包，负责令牌记录持久化、令牌作用域与类型建模以及令牌生命周期服务。
 *
 * <p>The package contains token entities, request and response models, repository and specification queries,
 * REST management services, listeners, initialization hooks, permission metadata, and lifecycle events for token records.
 * 该包包含令牌实体、请求响应模型、仓库与 Specification 查询、REST 管理服务、监听器、初始化钩子、权限元数据以及令牌记录的生命周期事件。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.rbac.token;

import org.jspecify.annotations.NullMarked;
