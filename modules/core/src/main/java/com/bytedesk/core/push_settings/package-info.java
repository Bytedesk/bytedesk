/**
 * push_settings package for push_settings dictionaries, push_settings assignment, and push_settings management services.
 * push_settings 包，负责标签字典、标签分配与标签管理服务。
 *
 * <p>Domain objects, repositories, query models, and import or export DTOs are implemented here.
 * 这里实现了领域对象、仓库、查询模型以及导入导出 DTO。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.push_settings;

import org.jspecify.annotations.NullMarked;
