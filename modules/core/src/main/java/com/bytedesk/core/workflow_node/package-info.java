/**
 * workflow_node package for workflow node definition persistence, node type modeling, and node service orchestration.
 * workflow_node 包，负责工作流节点定义持久化、节点类型建模与节点服务编排。
 *
 * <p>The package contains workflow node entities, request and response models, repository and specification queries,
 * node services, listeners, initialization hooks, and CRUD-oriented domain events for node lifecycle management.
 * 该包包含工作流节点实体、请求响应模型、仓库与 Specification 查询、节点服务、监听器、初始化钩子，以及面向节点生命周期管理的 CRUD 领域事件。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.workflow_node;

import org.jspecify.annotations.NullMarked;
