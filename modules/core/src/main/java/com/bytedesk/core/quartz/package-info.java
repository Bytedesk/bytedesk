/**
 * quartz package for Quartz scheduler runtime wiring, scheduled jobs, and schedule-event publication.
 * quartz 包，负责 Quartz 调度运行装配、定时作业与调度事件发布。
 *
 * <p>The package contains Quartz configuration, shared scheduling constants, event publication support,
 * a generic Quartz job abstraction, and dedicated subpackages for schedule events and concrete scheduled jobs.
 * 该包包含 Quartz 配置、共享调度常量、事件发布支持、通用 Quartz 作业抽象，以及用于调度事件和具体定时作业的专门子包。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.quartz;

import org.jspecify.annotations.NullMarked;