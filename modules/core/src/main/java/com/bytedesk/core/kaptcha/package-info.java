/**
 * kaptcha package for captcha generation, validation, and related runtime configuration.
 * kaptcha 包，负责验证码生成、校验与相关运行配置。
 *
 * <p>REST controllers and application services expose package capabilities to upper layers.
 * REST 控制器与应用服务向上层暴露该包能力。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.kaptcha;

import org.jspecify.annotations.NullMarked;
