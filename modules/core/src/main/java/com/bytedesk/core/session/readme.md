# session

This package handles web session and cookie configuration for request-scoped identity handling.

## Implementation Notes

- Configuration classes, filters, or interceptors wire runtime behavior.
