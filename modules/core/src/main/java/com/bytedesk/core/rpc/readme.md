# rpc

This package manages internal gRPC service adapters and cross-module remote invocation support.

## Implementation Notes

- GrpcServerService provides the core server-side RPC adapter used to expose internal module capabilities across service boundaries.
