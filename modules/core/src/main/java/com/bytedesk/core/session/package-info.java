/**
 * session package for web session and cookie configuration for request-scoped identity handling.
 * session 包，负责面向请求作用域身份处理的 Web 会话与 Cookie 配置。
 *
 * <p>Configuration classes, filters, or interceptors wire runtime behavior.
 * 配置类、过滤器或拦截器负责装配运行时行为。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.session;

import org.jspecify.annotations.NullMarked;
