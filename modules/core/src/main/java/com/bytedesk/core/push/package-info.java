/**
 * push package for push record persistence, multi-channel push routing, and push strategy services.
 * push 包，负责推送记录持久化、多通道推送路由与推送策略服务。
 *
 * <p>The package contains push entities, request and response models, repository and specification queries,
 * routing and filter services, expire-cache support, permission metadata, and specialized subpackages for
 * channel-specific send services and authentication validation strategies.
 * 该包包含推送实体、请求响应模型、仓库与 Specification 查询、路由与过滤服务、过期缓存支持、权限元数据，以及用于渠道发送服务和认证校验策略的专门子包。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.push;

import org.jspecify.annotations.NullMarked;
