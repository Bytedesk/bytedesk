/**
 * workflow_edge package for workflow edge definition persistence, routing condition modeling, and edge service orchestration.
 * workflow_edge 包，负责工作流连线定义持久化、路由条件建模与连线服务编排。
 *
 * <p>The package contains workflow edge entities, request and response models, repository and specification queries,
 * edge services, listeners, initialization hooks, and CRUD-oriented domain events for transition lifecycle management.
 * 该包包含工作流连线实体、请求响应模型、仓库与 Specification 查询、连线服务、监听器、初始化钩子，以及面向流转生命周期管理的 CRUD 领域事件。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.workflow_edge;

import org.jspecify.annotations.NullMarked;
