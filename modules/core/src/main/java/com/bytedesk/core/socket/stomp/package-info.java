/**
 * stomp package for STOMP-over-WebSocket configuration, message interception, and STOMP transport event integration.
 * stomp 包，负责 STOMP over WebSocket 配置、消息拦截与 STOMP 传输事件集成。
 *
 * <p>The package contains STOMP configuration and controller entrypoints, event publishers,
 * and dedicated subpackages for handlers, interceptors, and listeners that coordinate WebSocket messaging behavior.
 * 该包包含 STOMP 配置与控制器入口、事件发布器，以及用于 handlers、interceptors、listeners 的专门子包，共同协调 WebSocket 消息行为。
 *
 * @author bytedesk.com
 */
@NonNullApi
package com.bytedesk.core.socket.stomp;

import org.springframework.lang.NonNullApi;
