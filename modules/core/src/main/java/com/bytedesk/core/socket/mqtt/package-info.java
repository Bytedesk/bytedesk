/**
 * mqtt package for MQTT runtime configuration, session handling, and MQTT transport event integration.
 * mqtt 包，负责 MQTT 运行时配置、会话处理与 MQTT 传输事件集成。
 *
 * <p>The package contains MQTT properties and constants, session and channel utilities, REST entrypoints,
 * event publishers, and dedicated subpackages for handlers, initializers, listeners, protocol logic, server runtime, and services.
 * 该包包含 MQTT 属性与常量、会话与通道工具、REST 入口、事件发布器，以及用于 handlers、initializers、listeners、protocol、server、service 的专门子包。
 *
 * @author bytedesk.com
 */
@NonNullApi
package com.bytedesk.core.socket.mqtt;

import org.springframework.lang.NonNullApi;
