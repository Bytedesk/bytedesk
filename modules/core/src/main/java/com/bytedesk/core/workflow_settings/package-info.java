/**
 * workflow_settings package for workflow configuration persistence, settings type modeling, and default settings management.
 * workflow_settings 包，负责工作流配置持久化、设置类型建模与默认设置管理。
 *
 * <p>The package contains workflow settings entities, request and response models, repository and specification queries,
 * settings listeners, initialization data, permission metadata, and CRUD-oriented domain events for configuration lifecycle handling.
 * 该包包含工作流设置实体、请求响应模型、仓库与 Specification 查询、设置监听器、初始化数据、权限元数据，以及面向配置生命周期处理的 CRUD 领域事件。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.workflow_settings;

import org.jspecify.annotations.NullMarked;
