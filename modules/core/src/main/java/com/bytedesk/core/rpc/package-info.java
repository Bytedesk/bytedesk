/**
 * rpc package for internal gRPC service adapters and cross-module remote invocation support.
 * rpc 包，负责内部 gRPC 服务适配与跨模块远程调用支撑。
 *
 * <p>The package currently centers on gRPC server-side adapters used to expose or bridge internal services across module boundaries.
 * 该包当前主要围绕 gRPC 服务端适配器展开，用于在模块边界之间暴露或桥接内部服务。
 *
 * @author bytedesk.com
 */
@NonNullApi
package com.bytedesk.core.rpc;

import org.springframework.lang.NonNullApi;
