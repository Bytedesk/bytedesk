/**
 * redis package for Redis runtime configuration, cache/queue/stream integration, and Redis-backed support services.
 * redis 包，负责 Redis 运行配置、缓存/队列/Stream 集成与基于 Redis 的支撑服务。
 *
 * <p>The package contains Redis configuration and serializer classes, Jedis and cluster-switch properties,
 * login-retry and generic Redis services, Redis event abstractions, and dedicated subpackages for cache, queue, and stream runtime integration.
 * 该包包含 Redis 配置与序列化类、Jedis 与集群切换属性、登录重试与通用 Redis 服务、Redis 事件抽象，以及用于 cache、queue、stream 运行集成的专门子包。
 *
 * @author bytedesk.com
 */
@NonNullApi
package com.bytedesk.core.redis;

import org.springframework.lang.NonNullApi;
