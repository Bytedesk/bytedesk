# exception

This package handles domain exceptions, i18n exception factories, and global exception handling.

## Implementation Notes

- The package currently provides lightweight support classes and extension points around this domain.
