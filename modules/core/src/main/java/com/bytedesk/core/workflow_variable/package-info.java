/**
 * workflow_variable package for workflow variable persistence, variable scope modeling, and step-level value access services.
 * workflow_variable 包，负责工作流变量持久化、变量作用域建模与步骤级取值服务。
 *
 * <p>The package contains workflow variable entities, request and response models, scope and type enumerations,
 * repository access, controller endpoints, and services used to read and write values across workflow execution steps.
 * 该包包含工作流变量实体、请求响应模型、作用域与类型枚举、仓库访问、控制器接口，以及用于跨工作流执行步骤读写变量值的服务。
 *
 * @author bytedesk.com
 */
@NullMarked
package com.bytedesk.core.workflow_variable;

import org.jspecify.annotations.NullMarked;
