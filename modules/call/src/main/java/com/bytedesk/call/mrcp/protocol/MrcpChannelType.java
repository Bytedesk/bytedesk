package com.bytedesk.call.mrcp.protocol;

/**
 * Minimal MRCP channel types used by the internal protocol layer.
 */
public enum MrcpChannelType {
    SPEECHSYNTH,
    SPEECHRECOG,
    RECORDER,
    SPEAKVERIFY
}